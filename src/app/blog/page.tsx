import BlogClient from "./BlogClient";

export const metadata = {
  title: "Blog | Opendex Web Services",
  description:
    "Notas editoriales de Opendex sobre identidad, seguridad, documentos, operaciones e infraestructura empresarial.",
};

export default function Blog() {
  return <BlogClient />;
}
